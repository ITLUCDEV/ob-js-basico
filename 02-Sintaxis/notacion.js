// NOTACIÓN

// ; -> Delimita el final de una línea
const b = 4;
b + 4;

// [] -> Se utiliza para listas, arreglos, arrays
const ar = [1, 2, 3, 4] // comienza por 0
console.log(ar[2])

// () -> Se utiliza para funciones
function suma(a, b){
    // Se escribe la función
}

// {} -> Se utiliza para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
} 

if (true){
    // contenido de la estructura
}

// . -> Se utiliza en los objetos para acceder a los atributos
movil.anchura

